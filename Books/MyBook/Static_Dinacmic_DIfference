სტატიკური და დინამიკური ცვლადები, სტატიკური ცვლადს როცა ვიყენებთ მაგალითად სხვა ფუნქციიდან
იგი თავდაპირველად არის საწყისი მნიშვნელობის მქონე შემდეგ როგორ მოდიფიკაციასაც ჩავუტარებთ იმ მნიშვნელობას მიიღებს
და შემდგომი გამოყენებისას იქნება არა საწყისი მნიშვნელობის არამედ მასში იქნება ბოლოს მინიჭებული მნიშვნელობა
ყოველი აღწერისას არის მუდმივად იმ მნიშვნელობის რაც აქვს მინიჭებული
მაგ:
#include<stdio.h>
int fun()
{
static int count = 0; //სტატიკური ცვლადი
count++;  // count ის მნიშვნელობის გაზრდა 1 ით, 
return count;
}

int main()
{
printf("%d ", fun()); // ყოველი ამ ფუნქციის გამოძახებისას მასში არსებული სტატიკური ცვლადი იქნება გაზრდილი
printf("%d ", fun()); // მიუხედავად იმისა რომ ფუნქციაში აღიწერება მასში არსებული ცვლადი და ენიჭება 0
printf("%d ", fun()); // ფუნქციაში არსებული ცვლადი აღწერისას იღებს მნიშვნელობას და თუ კომპილერი მას რამდენიმეჯერ გაივლის
printf("%d ", fun()); // მისი მნიშვნელობა არ იქნება საწყისი, ანუ როცა აღწერა და მინიჭება ხდება
printf("%d ", fun()); // როცა კომპილერი დინამუკურ ცვლადს გაივლის ყოველ ჯერზე ცვლადი მიიღებს იმ მნიშვნელობას რაც აღწერაში აქვს
return 0;
}
